OPENAI_API_KEY=sk-your-key-here
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Local dev: leave as-is if running docker-compose (qdrant service).
# Cloud: paste the URL + API key from your free Qdrant Cloud cluster.
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

COLLECTION_NAME=knowledge_base
CHUNKS_STORE_PATH=./data/chunks.json
